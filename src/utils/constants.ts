// 常用数据验证规则
export const REG = {
  MOBILE: /^[0-9]{11}$/,
  PASSWORD: /^(?!\d+$)(?![A-Za-z]+$)[a-zA-Z0-9]{8,16}$/, // 8-16 位英文和数字（英文数字都要有，忽略大小写）
  NUMBER: /^-?\d+(.\d+)?$/,
  IDCARD: /^[a-z0-9A-Z]+$/,
  URL: /^https?:\/\//,
  IP: /^([0-9]{1,3})(\.[0-9]{1,3}){3}$/
}
